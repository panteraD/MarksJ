/*
 * Created on 17 May 2016 ( Time 04:14:29 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package org.marks.test.persistence;


import org.marks.bean.jpa.GroupsEntity;
import org.marks.mock.GroupsEntityMock;
import org.marks.persistence.PersistenceServiceProvider;
import org.marks.persistence.services.GroupsPersistence;

import org.junit.Assert;
import org.junit.Test;

/**
 * JUnit test case for Groups persistence service
 * 
 * @author Telosys Tools Generator
 *
 */
public class GroupsPersistenceTest 
{
	@Test
	public void test1() {
		
		System.out.println("Test count ..." );
		
		GroupsPersistence service = PersistenceServiceProvider.getService(GroupsPersistence.class);
		System.out.println("CountAll = " + service.countAll() );
	}
	
	@Test
	public void test2() {
		
		System.out.println("Test Groups persistence : delete + load ..." );
		
		GroupsPersistence service = PersistenceServiceProvider.getService(GroupsPersistence.class);
		
		GroupsEntityMock mock = new GroupsEntityMock();
		
		// TODO : set primary key values here 
		process( service, mock, 0  );
		// process( service, mock, ... );
	}

	private void process(GroupsPersistence service, GroupsEntityMock mock, Integer id ) {
		System.out.println("----- "  );
		System.out.println(" . load : " );
		GroupsEntity entity = service.load( id );
		if ( entity != null ) {
			// Found 
			System.out.println("   FOUND : " + entity );
			
			// Save (update) with the same values to avoid database integrity errors  
			System.out.println(" . save : " + entity );
			service.save(entity);
			System.out.println("   saved : " + entity );
		}
		else {
			// Not found 
			System.out.println("   NOT FOUND" );
			// Create a new instance 
			entity = mock.createInstance( id ) ;
			Assert.assertNotNull(entity);

			// This entity references the following entities : 
			// . Specialities
			// . Specialities
			/* Insert only if references are OK
			// Try to insert the new instance
			System.out.println(" . insert : " + entity );
			service.insert(entity);
			System.out.println("   inserted : " + entity );
			*/

			System.out.println(" . delete : " );
			boolean deleted = service.delete( id );
			System.out.println("   deleted = " + deleted);
		}		
	}
}
